program qqq
    logical:: flag(4)
    character(len=20)::ww
    integer,pointer :: q2
    complex(kind=8),target:: a(4,4),q,c(16)
    complex(kind=8),pointer:: p(:),p2(:,:)
    flag(:)=.false.
    flag(2)=.true.
    if(flag(:)) write(*,*) 'qq'
        
end program qqq

!!$ function convptrtoi(source) result(target)
!!$     integer, intent(in), target :: source
!!$     integer, pointer :: target
!!$     target=>source
!!$ end function convptrtoi